"""Domain tools for the template agent."""

from __future__ import annotations

def example_tool(input_text: str) -> str:
    """Replace with database, API, search, or file-processing logic."""
    return f"Processed: {input_text}"
